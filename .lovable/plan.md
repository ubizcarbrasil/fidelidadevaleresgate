

## Problema

A edge function `mirror-sync` foi criada no código mas nunca foi deployada. Por isso o erro "Failed to send a request to the Edge Function".

## Solução

1. **Deployar a edge function `mirror-sync`** — usar a ferramenta de deploy para publicar a função que já existe em `supabase/functions/mirror-sync/index.ts`.

Nenhuma alteração de código necessária. Apenas o deploy da função existente.

